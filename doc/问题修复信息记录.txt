
/////////////////////////////////////////////////////////////////////////////////////////////////////////

检查项目，把发现的问题全部列出来，并深度解读每个问题细节，最后保存到根目录

/////////////////////////////////////////////////////////////////////////////////////////////////////////

claude --resume 2056b7d2-0f03-4b59-bf73-cfc22dc88edc


/////////////////////////////////////////////////////////////////////////////////////////////////////////

1. 修复 ./项目问题报告1.md 中列举的未完成的全部问题;
2. 修复发现完的每一个问题都记录下你能识别的完成信息，重启任务时判断要不要重新做;
3. 任务启动前务必检查已经完成的信息，避免重复;
4. 修复完直接构建并修复编译错误，直到问题全部修复为止。


/////////////////////////////////////////////////////////////////////////////////////////////////////////


1. 修复 ./项目审查报告.md 中列举的未完成的全部问题;
2. 修复发现完的每一个问题都记录下你能识别的完成信息，重启任务时判断要不要重新做;
3. 任务启动前务必检查已经完成的信息，避免重复;
4. 修复完直接构建并修复编译错误，直到问题全部修复为止。


/////////////////////////////////////////////////////////////////////////////////////////////////////////

构建:
1.构建并运行，出现错误直接修复，直到问题全部修复为止
2. 构建并运行（需要运行起来判断构建是否成功），有错误直接修复，不要停，直到问题全部修复为止。


/////////////////////////////////////////////////////////////////////////////////////////////////////////

运行编译出来的exe，没有重新开启修复任务，有错误直接修复，直到能编译出正确的exe


/////////////////////////////////////////////////////////////////////////////////////////////////////////

1. 请验证PhysicsSyncClientMain.exe发送到PhysicsSyncServerMain.exe的消息是否正常，如果不正常，请设修复;
2. 同时验证PhysicsSyncServerMain.exe发送到PhysicsSyncClientMain.exe的消息是否正常，如果不正常，请设修复;
3. 修复完运行编译出来的exe，没有重新开启修复任务，有错误直接修复，直到能编译出正确的exe;


/////////////////////////////////////////////////////////////////////////////////////////////////////////


/////////////////////////////////////////////////////////////////////////////////////////////////////////


1. 请验证PhysicsSyncClientMain.exe发送到PhysicsSyncServerMain.exe的消息是否正常，如果不正常，请设修复;
2. 同时验证PhysicsSyncServerMain.exe发送到PhysicsSyncClientMain.exe的消息是否正常，如果不正常，请设修复;
3. 测试必选项：要启动两个或多个客户端，验证每个客户端是否正确收到了其他客户端的消息，如果没收到消息或收到的消息不正确，请设修复;；
3. 修复完运行编译出来的exe，没有重新开启修复任务，有错误直接修复，直到能编译出正确的exe，并且验证的结果都是正确的;

/////////////////////////////////////////////////////////////////////////////////////////////////////////



1. 请验证PhysicsSyncClientMain.exe发送到PhysicsSyncServerMain.exe的消息是否正常，如果不正常，请设修复;2. 同时验证PhysicsSyncServerMain.exe发送到PhysicsSyncClientMain.exe的消息是否正常，如果不正常，请设修复;3. 测试必选项：要启动两个或多个客户端，验证每个客户端是否正确收到了其他客户端的消息，如果没收到消息或收到的消息不正确，请设修复;4. 修复完运行编译出来的exe，没有重新开启修复任务，有错误直接修复，直到能编译出正确的exe，并且验证的结果都是正确的


/////////////////////////////////////////////////////////////////////////////////////////////////////////

